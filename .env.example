# 8bit-Cafe Dashboard - Environment Variables Template
# Copy this file to `.env` and fill in the values for your local environment.

# The base URL of the backend API service.
# For local development with a tunnel (e.g., ngrok), use the tunnel URL.
VITE_API_URL=https://your-backend-api-subdomain.ngrok-free.dev
